
const renderAboutus = (req, res) => {
    res.render('About-us');
};

const renderNewEntry = (req, res) => {
    
};

const createNewEntry = (req, res) => {
    
};

module.exports = {
    renderAboutus,
    renderNewEntry,
    createNewEntry
};